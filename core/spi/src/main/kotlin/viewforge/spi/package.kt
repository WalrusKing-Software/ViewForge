package viewforge.spi

/**
 * core/spi: the framework-package interfaces (FrameworkPackage, ComponentDefinition,
 * CodeGenerator, TargetDefinition — ARCHITECTURE §6). Nothing here may mention Compose (ADR-007).
 * Interfaces land at M1/M6; marker only for M0.
 */
internal const val MODULE = "core:spi"
