package viewforge.command

/**
 * core/command: the command pattern (apply/invert), undo/redo stacks, and transaction grouping
 * (ARCHITECTURE §5, CLAUDE.md rule 3). Implementation lands at M4; marker only for M0.
 */
internal const val MODULE = "core:command"
