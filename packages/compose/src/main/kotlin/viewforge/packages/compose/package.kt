package viewforge.packages.compose

/**
 * packages/compose: the Compose framework package — component/modifier definitions, runtime
 * renderers, KotlinPoet emitters, and target exporters (ARCHITECTURE §6, ADR-008). Source
 * packages `components`, `modifiers`, `codegen`, and `targets` fill in from M2/M6. Marker only
 * for M0.
 */
internal const val PACKAGE_ID = "compose-multiplatform"
