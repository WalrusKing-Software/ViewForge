package viewforge.editor.state

/**
 * editor/state: EditorState (document, selection, history, viewport) exposed as Compose state
 * (ARCHITECTURE §5). Implementation lands at M3+; marker only for M0.
 */
internal const val MODULE = "editor:state"
