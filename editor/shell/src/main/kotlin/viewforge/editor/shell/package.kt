package viewforge.editor.shell

/**
 * editor/shell: window, menus, dialogs, theming — the assembly point for panels and canvas
 * (ARCHITECTURE §2). Implementation lands at M2+; marker only for M0.
 */
internal const val MODULE = "editor:shell"
