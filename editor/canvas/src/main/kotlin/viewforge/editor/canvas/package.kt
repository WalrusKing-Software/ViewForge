package viewforge.editor.canvas

/**
 * editor/canvas: live IR rendering, the spatial index for hit-testing, and the overlay for
 * selection/hover/drop chrome (ARCHITECTURE §4, ADR-009). Implementation lands at M2/M3; marker
 * only for M0.
 */
internal const val MODULE = "editor:canvas"
